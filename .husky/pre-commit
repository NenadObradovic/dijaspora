# Windows Git Bash + nvm cannot reliably exec the Unix `npm` shim.
# Run via cmd.exe so npm.cmd is used.
if [ -n "$WINDIR" ] || [ -n "$MSYSTEM" ]; then
  cmd.exe //c "npm run lint && npm run format:check"
else
  npm run lint && npm run format:check
fi
